# Assisted by: Gemini 3
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: compute-jobs.coldpress.io
spec:
  group: coldpress.io
  names:
    kind: ComputeJ
    plural: compute-jobs
    singular: compute-job
    shortNames: ["cmpj"]
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              x-kubernetes-preserve-unknown-fields: true # Allows arbitrary config structure
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: discovery-jobs.coldpress.io
spec:
  group: coldpress.io
  names:
    kind: DiscoveryJ
    plural: discovery-jobs
    singular: discovery-job
    shortNames: ["dscj"]
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                template:
                  type: string
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: workload-templates.coldpress.io
spec:
  group: coldpress.io
  names:
    kind: WorkloadTemplate
    plural: workload-templates
    singular: workload-template
    shortNames: ["wt"]
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                requirements:
                  type: object
                  properties:
                    num_nodes: {type: string}
                    gpus_per_node: {type: string}
                    roce_nics_per_node: {type: string}
                user_params:
                  type: array
                  items: {type: string}
                allocator_params:
                  type: array
                  items: {type: string}
                annotations: 
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                image: {type: string}
                command: {type: array, items: {type: string}}
                args: {type: array, items: {type: string}}
                env: 
                  type: array
                  items:
                    type: object
                    properties:
                      name: {type: string}
                      value: {type: string}
                blocking:
                  type: object
                  properties:
                    type: {type: string, enum: ["completion", "endpoint", "delay"]}
                    address: {type: string}
                    delay: {type: integer}
                files_to_copy: {type: array, items: {type: string}}
                ephemeral_mounts:
                  type: array
                  items:
                    type: object
                    properties:
                      target: {type: string}
                      size: {type: string}
                sys_mounts:
                  type: array
                  items:
                    type: object
                    properties:
                      source: {type: string}
                      target: {type: string}
                      read_only: {type: boolean}
                resources: {type: object, x-kubernetes-preserve-unknown-fields: true}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: discovery-templates.coldpress.io
spec:
  group: coldpress.io
  names:
    kind: DiscoveryTemplate
    plural: discovery-templates
    singular: discovery-template
    shortNames: ["dt"]
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                image: {type: string}
                script: {type: string}
                result_dir: {type: string}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: coldpressresourceallocators.coldpress.io
spec:
  group: coldpress.io
  names:
    kind: ColdpressResourceAllocator
    plural: coldpressresourceallocators
    singular: coldpressresourceallocator
    shortNames: ["cra"]
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              x-kubernetes-preserve-unknown-fields: true
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true