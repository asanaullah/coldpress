# Assisted by: Gemini 3
apiVersion: coldpress.io/v1
kind: ColdpressResourceAllocator
metadata:
  name: gromacs-md-run
  namespace: researcher-a
spec:
  storage:
    results: researcher-a-storage
  tasks:
    - name: "water-benchmark-4gpu"
      template: "gromacs-parser"
      params:
        num_gpus: 4            # Used by Allocator
        npme: 1 
        benchmark_url: "https://ftp.gromacs.org/pub/benchmarks/water_GMX50_bare.tar.gz"
        dataset_dir: "water-cut1.0_GMX50_bare/1536" 
        steps: 10000