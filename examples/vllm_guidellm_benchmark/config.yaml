# Generated by: Claude Sonnet 4.5
project: researcher-a

# Per-task discovery - only run on task 0 (inference server with GPU)
# Task 1 (benchmark client) doesn't need hardware discovery
discovery:
  template: user_snapshot
  tasks: [0]  # Only task 0 runs discovery

output: vllm-benchmark-job
