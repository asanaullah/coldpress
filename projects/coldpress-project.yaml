# Generated by: Claude Sonnet 4.5
namespace: coldpress-project

# Target platforms - determines which RBAC permissions to grant
# Options: jobset, kubeflow, or both (comma-separated)
targets: jobset,kubeflow

# References to cluster-wide resources
cluster_queue: coldpress-cluster-queue
storage_class: nfs-csi

# Storage configuration
storage:
  results: coldpress-project-storage
  size: 500Gi
